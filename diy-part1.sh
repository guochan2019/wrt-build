#!/bin/bash
# diy-part1.sh — 在 feeds update 前执行
# 用途：feeds 源定制（添加/删除/修改 feed）

# 如果 feeds.conf.default 中已包含所有 feed 源，此脚本可留空
# 如需额外调整可在此添加

# 示例：删除默认 feed（如需）
# sed -i '/oldpackages/d' feeds.conf.default

exit 0
